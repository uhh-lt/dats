{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run DATS Setup",
      "type": "shell",
      "command": "${command:python.interpreterPath}",
      "args": ["src/setup.py"],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "presentation": {
        "reveal": "always",
        "panel": "shared",
        "clear": true
      }
    }
  ]
}
