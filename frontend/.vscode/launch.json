{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "ui",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run dev",
      "cwd": "${workspaceFolder}",
      "envFile": "${workspaceFolder}/frontend/.env"
    }
  ]
}
