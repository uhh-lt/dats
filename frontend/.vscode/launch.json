{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "frontend",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run dev",
      "cwd": "${workspaceFolder}",
      "envFile": "${workspaceFolder}/frontend/.env"
    }
  ]
}
