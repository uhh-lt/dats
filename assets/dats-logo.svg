<svg id="dats-logo" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" width="64" height="64" class="">
                    <!-- Shifted all elements right by +7 and down by +0 to center the 50x64 bounding box inside 64x64 -->
                    <!-- Dot 1: radius 8 (width 16) -->
                    <circle cx="25" cy="8" r="8" fill="white"/>
                    <!-- Dot 2: radius 6 (width 12) -->
                    <circle cx="43" cy="22" r="6" fill="white"/>
                    <!-- Dot 3: radius 10 (width 20) -->
                    <circle cx="23" cy="32" r="10" fill="white"/>
                    <!-- Dot 4: radius 7 (width 14) -->
                    <circle cx="50" cy="41" r="7" fill="white"/>
                    <!-- Dot 5: radius 9 (width 18) - This was the one clipping -->
                    <circle cx="32" cy="55" r="9" fill="white"/>
                    <!-- Dot 6: radius 5 (width 10) -->
                    <circle cx="12" cy="51" r="5" fill="white"/>
                    <!-- Dot 7: radius 3 (width 6) -->
                    <circle cx="12" cy="17" r="3" fill="white"/>
                </svg>
